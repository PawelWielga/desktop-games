# System icons

This folder contains custom SVG icons for internal shell/system apps.

Current assets are original project icons created directly for this repository. Keep `icon` fallbacks in `src/window/registry.ts` for graceful degradation.
